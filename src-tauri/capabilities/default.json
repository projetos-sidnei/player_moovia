{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and the player overlay",
  "windows": ["main", "player-overlay"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
